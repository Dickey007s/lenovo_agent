# 系统演示图片

## 最新交付：图内标注

用户纠正后，正式交付改为 `in-image/` 内的四张 PNG，压缩包为 `系统演示图片-图内标注-4张.zip`。
标注直接放在原截图内部，用红色箭头指向对应模块；没有外侧说明栏，也没有扩展截图画布。
输出是原图等比例 2 倍尺寸：前三类横屏为 3344 × 1880，蜂群图为 2880 × 2200。
采用 `build-in-image.mjs` 重建；在图内空白区域放置说明，检查正文与按钮没有被遮挡。
未标注像素与原图的同尺寸缩放结果逐像素一致，校验记录为 `.build/in-image-verification.json`。

以下内容保留第一版制作记录，`images/` 是旧版外侧说明图，不再作为本次最终交付。

制作日期：2026-09-13。用途：向老师说明原系统的操作方式和各模块含义。

四张图片都是 3840 × 2160 PNG，蓝色 A-F 与右侧说明一一对应，顶部给出本页操作流程。
按照用户确认，不使用 imagegen 重绘；原截图只做等比例缩放，另外叠加字母与模块角标。原始文件副本保存在 `sources/`。

## 四个视图

1. **任务进展**：默认工作进展、当前待办、分支、已有成果。
2. **执行记录**：按轮查看资料、证据门、局部处理和模型调用回执。
3. **蜂群协作**：服务端准入、受限 Worker、工作包依赖、返回与采用。
4. **证据核对**：问题原因、影响范围、真实候选位置、主动选择与确认。

整体入口仍是办公资料库和 `/agent-capabilities`。用户先在原系统提交自己的办公目标，再按需要查看上述页面；它们是同一任务的不同视图，不是四步必经流程，更不是四套 Runtime。

## 来源与事实范围

- 图片 01、02、04：`../demo12-polish-20260911/screenshots/` 中的 `progress-1672.png`、`record-1672.png`、`evidence-1672.png`。
- 图片 03：`../../evidence/screenshots/dr-0063-fixture-swarm-1440.png`。
- 来源证据：`../demo12-polish-20260911/README.md`、`../../evidence/DR-0063-INTEGRATED-BOUNDARIES-AND-SWARM-FACTS-20260912.md`。
- 行为核验：`../../../apps/web/app/harness-workbench.tsx` 中的 `CapabilityProgress`、`CapabilityExecutionRecord`、`CollaborationOverview`，以及 `../../contracts/UI_SERVER_FACT_MATRIX.md`。

截图展示真实 React 组件加载受控 Snapshot 的状态，不是本次真实模型运行。第三张与其他图片为不同测试状态，不能拼成同一次成功任务轨迹。截图中的数据、工作包和文字均保持原样。

标注不引入任何新入口、状态或能力：确认依据不代表业务批准；已调用不代表已采用；就绪不代表已经执行；有成果不代表全部任务完成；终态续办要创建同任务的新 Run。

本次没有修改产品源码、触发模型调用、重跑全量产品回归或开展目标用户研究。交付检查包括四图尺寸、每图六个模块映射、逐图视觉检查，以及声明标注区域之外的截图像素对比。

## 文件

- `images/`：四张正式 PNG。
- `系统演示图片-4张.zip`：仅四张正式 PNG，方便转交。
- `index.html`：四图总览，不是产品新界面。
- `annotations.json`：可修改的操作和模块文案。
- `build.mjs`：可复现的本地生成脚本。
- `.build/verification.json`：尺寸与像素保真检查记录。
