# 场景效果账本：Agent 最终给用户交付了什么

这份账本用于汇报“系统产生了什么可检查影响”，而不是再用架构名、`completed` 或引用数量代替效果。当前资料全部来自 FORTE 固定公开输入，原始文件只读。

## 汇报口径

每条场景分三层讲：

1. **模型层**：Planner/Analyst 是否真实调用，返回是否被采用。
2. **效果层**：是否生成真实文件，数值、字段、排序、规则或测试由哪个确定性 Validator 复核。
3. **控制层**：Run 是 completed、waiting_input、failed 还是外部边界；已经保留什么，下一步由谁决定。

三层不能互相替代。模型未采用不删除已验证文件；Artifact 通过也不把额外分支写成 completed。

## 十五个场景

| ID | 用户任务 | 真实成果 | 确定性检查 | 当前结论 | 禁止副作用 |
| --- | --- | --- | ---: | --- | --- |
| TC-01 | 按入职表和规则匹配资产权限 | `入职资产匹配表.csv` | 5 | 通过；日期/排序/隐私/表头/分隔符及 PDF 关键词、优先级、多备注规则均进入固定检查 | 不覆盖名单、不建账号/采购单 |
| TC-02 | 将固定搜索 Workflow 重构为有界 ReAct 控制结构 | 完整项目 ZIP + `TC-02测试与改动说明.md` | 12 个唯一检查，两份成果共享 | 通过，当前 20 项声明/执行 unittest 一致；默认策略确定性执行已规划工具，可替换但非模型自主 ReAct | 不改原始源码；固定测试未调用生产搜索；外层模型回执不冒充包内策略 |
| TC-03 | 连接远程 Datasette 做经营分析 | 无 | 0 | `blocked_external_boundary` | 不伪造 SQL、不连未授权库 |
| TC-04 | 为评测平台补测试并修缺陷 | 完整 44 文件 ZIP + `TC-04真实测试报告.md` | 12 个唯一检查，两份成果共享 | `Limited Verified`：旧 105 项替身 false green 与真实未修复副本 5 个红灯均保留；修复后 117/117，三份变更源码 coverage 为 97.9%/97.8%/89.2%；真实 Run 的 Artifact 效果通过但 Analyst 未采用、整轮 failed；长构建期间 Run GET 最大 47ms、health 最大 16ms、SSE 可立即读取 started 事实 | 不改原始源码、不访问真实 endpoint、不运行前端脚本、不自动 PR；线程与子进程不可跨进程续跑 |
| TC-05 | 核对三期往来和风险候选 | 2 个 2026 CSV + 三期说明 MD | 15 个来源与成果检查 | `Limited Verified`（DR-0046）：当前批准来源动态为未付 31 条/`3984606.46`、未收 2 条/`4992891.47`、候选 0 条；正向来源变体生成 1 条候选仍通过，候选与财务处置分层 | 候选不是僵尸账款定论；不付款、核销、记账、确认坏账或改原件 |
| TC-06 | 两岗位分别审阅五份简历 | 2 个结构化 DOCX + 110 行 CSV | 11 个唯一检查，三份成果共享 | `Limited Verified`（DR-0045）：七份来源合同、四状态双来源台账、来源/阈值变异、真实 Provider、下载解析和 PostgreSQL 复读通过；当前 32/6/71/1，最终 10 组岗位匹配均待 HR 决定 | 不自动录用/淘汰，不写 ATS/通知，不声称无偏，隐藏非必要联系与人口属性值 |
| TC-07 | 六份委托书风控核查 | `授权委托书风控报告.docx` + `授权委托书逐项核查台账.csv` | 11 个唯一检查，两份成果共享 | `Limited Verified`（DR-0044、PR #59）：21×6 来源推导台账、三状态前台、真实 Provider、下载解析和 PostgreSQL 复读通过；固定输入为 6 份高风险、11 项关键资料不足、0/6 签署证据，3/3 法务 Gate 失败；repaired 变体动态投影 5/6 与 1/6，无旧固定数量 | 不替代法律意见、不验证签名/律师资格、不签署或认定授权生效 |
| TC-08 | 从 Web 采集竞品广告 | 无 | 0 | `blocked_external_boundary` | 不访问未授权 Web、不造 URL |
| TC-09 | 定时搜索资讯并追加文件 | 无 | 0 | `blocked_external_boundary` | 不建 cron、不访问未授权 Web |
| TC-10 | 从批准说明推导 M1 合规外呼流程 | 结构化 DOCX | 12 个来源/DOCX/图结构检查 | `Limited Verified`（DR-0047）：当前来源动态为 15 组/34 条规则，31 节点、36 边、7 守卫、7/7 终态可达；参数与新增转人工来源变体会改变图，未知规范、不可达终态和 DOCX 篡改转红；最终审批仍待人工 | 不拨号、不写 CRM/短信、不写禁呼名单、不实际转人工；不是法律意见或最新监管验证 |
| TC-11 | 汇总 PRD/配置/测试判断上线 | DOCX | 8 | 通过，确定性结论“不上线” | 不执行上线、不改配置 |
| TC-12 | 看板工具库补 Vitest 并修复 | ZIP + `TC-12真实测试报告.md` | 12 个唯一检查，两份成果共享 | 同测集 Stage A/B/C 红灯，Stage D 71/71；逐文件 coverage 与独立复跑通过 | 不改原始源码、不联网安装、不自动 PR；无 OS 级断网 |
| TC-13 | 从批准问卷与规则推导画像清洗和策略草案 | Markdown + 逐原始行 CSV | 8 个来源/成果检查，两份成果共享 | `Limited Verified`（DR-0049）：当前来源动态 11/10/1/8/2/3、画像 3/3/2、priority witness 0；阈值/优先级/新样本变异动态生效，策略来源仍为 `no_approved_strategy_source` | 公开样本非客户研究；重复口径待批准；不联系客户、不写 CRM/商机、不触发营销 |
| TC-14 | 从固定日志推导离线复盘与条件式止损提案 | Markdown + 观察/动作 CSV | 12 个来源/成果/安全边界检查，两份成果共享 | `Limited Verified`（DR-0050、PR #66、Run `harness:fe527536c857404f88f46d9a68b09397`）：当前来源动态 232 行、167 条观察、3 组来源冲突、2 个假设、8 个 ES + 3 个业务提案；target 全部 unresolved/not_applicable、approval required、executed=false；合法指标/冲突变异、篡改转红、下载解析和 PostgreSQL 顺序恢复通过 | 不连接或执行 Elasticsearch/HTTP 命令，不把相关性写成根因定论，不选 dedicated master，不执行限流/降级或生产审批 |
| TC-15 | 从完整交互日志、规则与页面规范推导可回溯排序 | 聚合优先级 CSV + 逐行归因 CSV | 12 个来源/覆盖/规则引用/双成果/无动作检查，两份成果共享 | `Draft`（DR-0051）：本地来源变异、篡改门和前台 canonical/动态/失败已通过；真实 Provider/PostgreSQL/PR 收尾中。当前来源动态 212 行、87 组、P0-P4=25/40/14/6/2；逐组保留内容寻址 rule refs | 不把 bounded Preview 当完整来源，不把映射假设或方案模板冒充批准结论，不修改/发布生产界面或启动实验 |

三个代码场景的命令由服务端固定，运行在一次性临时目录中，网络代理被清空，子进程只接收启动 Python/Node 所需的最小环境变量；API Key、Token、数据库 DSN、`PYTHONPATH` 和用户 shell hook 不会传入。这里证明的是固定测试包，不是可执行任意用户代码的生产沙箱，也不是 OS 级 socket 隔离。

## 前台如何让用户看见 Agent 的影响

- 成果文件以独立区域出现，显示文件名、类型、大小和检查通过数，不埋在聊天长文中。
- 点击“查看逐项检查”可以看到每条代码复核事实；点击“查看效果回执”可以看到 Agent 做了什么、观察到什么、花了什么、结果是什么。
- 下载按钮读取真实 Artifact bytes；没有 Artifact 的外部边界不显示按钮。
- “原始 FORTE 文件没有被修改”和禁止副作用直接可见。
- 右侧模型回执继续显示 called/output_used/elapsed；它不被成果卡的绿色状态覆盖。
- 如果额外分支仍待处理，用户仍能从 Branch lane 处理；已验证成果不会消失。
- 当已验证成果与来源定位缺口同时存在时，成果和下载先出现；同一来源影响的多个内部 Gap 合并为一个“不影响成果”的审计项，但底层 Branch 和 Run 状态不改写。
- TC-06 先显示“这是人工复核建议，不是录用或淘汰决定”，再分开显示确定性来源/成果检查、岗位建议和 HR 待决；条件展开同时展示 JD 与简历位置，`unverifiable` 不会被前端翻译为不满足。

## 从红灯到通过

1. 首次六场景 live：`0/6`，模型已调用但工具被 Analyst 结构门挡住。
2. 调整执行顺序：`6/6` 生成成果，但只 1 条 Analyst 输出采用。
3. 简化 Analyst 输出合同：TC-05、TC-13 探针均 completed。
4. 增加唯一安全 Unit 重绑：TC-10 completed；TC-15 的模型输出被采用并保留 waiting Branch。
5. 当前六个优先场景均有真实模型采用和效果通过记录；TC-14/15 仍保留额外模型分支等待，这个边界不被涂绿。
6. TC-02 旧门曾把缺真实 `workflow.py/search_agent.log` 的 9 文件迷你包判绿。DR-0040 保留该红灯，改为复制七个真实输入、下载后独立复测，并让声明与执行测试 ID 一致后才通过。
7. TC-04 旧门曾把 105 项替身函数测试判绿。DR-0041 分开保留该 false green 与完整未修复副本的五个真实红灯；修复后五类 117 项测试、逐文件覆盖率和前台公开 ID 集合才共同决定效果状态。
8. TC-04 总运行约一分钟不代表 API 可以一分钟不可用。`deterministic_office_tool_started` 必须在工作线程开始前进入 Snapshot/SSE；受控阻塞期间资料库、health 与 Run GET 仍可读。客户端 180 秒只作为总任务容忍，不是响应性修复，也不证明多 Worker 或 durable tool execution。
9. TC-04 最终 live Run `harness:c4bd926b13a44665aa49429d177305a9` 总耗时约 165 秒；下载 ZIP 独立复跑 117/117。确定性效果通过与 Run `failed` 同时保留，说明成果事实、模型采用和 Control Loop 终态不能互相替代。
10. TC-12 旧门只有修复后 9/9，缺少能证明缺陷的未修复红灯。DR-0042 保留该历史基线，并用同一 71 项真实 Vitest 依次复现别名、增长率、排序与日期边界问题；修复后 71/71、三个变更业务模块逐文件 coverage 过门，页面/ZIP/实际 collected ID 同集。
11. TC-07 旧门写死每份风险答案并强制排除 R05，属于固定样本自绿。DR-0044 改为解析 21 条来源规则与六份 DOCX 包；空签署栏无包内签署对象时触发 R05，律师证号无 Registry/Connector 回执时保持不可验证，字段串线、来源变异和成果篡改均进入负向门。
12. TC-06 旧门保存固定姓名对应的岗位答案，来源变异后仍可能自绿。DR-0045 改为从两份 JD 和五份简历生成 110 条双来源判断；孙博文经历、JD 年限、李雨桐 BD 事实和王琳达例外条款变异只改变受影响范围，隐私泄漏、字段串线和成果篡改转红。
13. TC-05 旧门把“候选必须为空”混入成功条件，并用生成前内存列表核对自己。DR-0046 改为重读三份批准 XLSX 与三份成果 bytes；0/N 条候选都可计算通过，绵阳长城余额变体产生 1 条候选而不转红，金额、方向、locator、候选集合和旧固定文案篡改则转红。
14. TC-10 旧门写死流程、终态和 13 项检查后自证，还把录音放在身份确认之前。DR-0047 改为从唯一批准 Markdown 生成规则账本和可遍历图，再独立解析 DOCX；合法时段、频次、保存年限、顺序和转人工规则变异动态生效，未知规范与图缺口不能静默绿色。
15. TC-13 旧门写死样本 ID、8 条分类、排除名单、阈值、优先级和销售话术后自证。DR-0049 改为重读批准 CSV/Markdown 与两份成果 bytes；当前 11/10/1/8/2/3 和 3/3/2 只是来源观测，canonical 0 个优先级 witness 也如实显示。阈值、多标签、新样本和 ID 变异动态生效，未知规范、CSV 注入与成果篡改转红；没有批准来源的策略只保留待负责人补充模板。
16. TC-14 旧门只提取 IP，其余 QPS、48 个 UNASSIGNED、根因、命令和 9 项检查由固定字符串自证。DR-0050 改为逐行重算 observation/conflict/hypothesis/proposal，并重读最终 Markdown/CSV。三组来源冲突不再被绿色文件覆盖；合法来源变异动态生效，所有 target 未解析、所有动作未执行，官方文档只解释 API 语义。
17. TC-15 旧门只分析 120 行 Preview 并产出 66 组，漏掉完整 212 行中的 21 个尾部独有组，22 个重叠组频次也发生变化，但 6/6 仍绿。DR-0051 改为完整原始 XLSX、来源规则/规范推导、逐组 content-addressed rule refs 与双 CSV 重读验证；当前 87 组和分布只是来源观测，不是生产 success 常量。

## 汇报限制

这套实现证明的是固定 FORTE 数据、固定办公适配器和当前 Runtime 的效果闭环。它不证明任意任务泛化、真实企业数据、用户理解、生产安全沙箱、分布式 Worker、外部 Connector 或业务价值。Demo 2 的多 Worker 自组织和 Demo 3 的跨拓扑风险许可仍是目标能力。
