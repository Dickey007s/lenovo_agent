# USER-FEEDBACK-20260826-19：继续实现可恢复 Control Loop

## 原始触发

在连续要求“Agent 自己研究文件仓库，以 loop 方式提出下一步，再由人确定”、
并再次确认产品名称必须保持 `Agent Control Loop` 后，Stakeholder 询问下一步。
项目给出的下一纵切是：把轮次间 Evidence Gate 做成人工确认点，补服务端
Snapshot 恢复和逐轮成果版本。Stakeholder 在当前任务中回复：“执行”。

## 支持的判断

- 可以继续实现上述 Agent Control Loop 纵切，而不是另起 Demo 专用 Runtime。
- 轮次间补证不能静默发生；需要把“继续花一轮预算”变成可见的人类决定。
- 恢复与成果演进必须同时落到前台状态和服务端事实，而不只做后台工程。

## 局限

- “执行”是实现授权，不是目标用户研究，也不证明新交互更易懂、更可信或更高效。
- 本记录不授权宣称多实例高可用、在途模型调用续跑、真实文件写入、真实 Tool
  Gateway 或外部动作已经实现。
- 具体实现与验证结论仍须回到 `DR-0025`、`SCENARIO-011` 和对应 Evidence。
